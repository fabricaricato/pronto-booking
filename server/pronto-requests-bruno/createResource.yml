info:
  name: createResource
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4000/api/resources
  body:
    type: json
    data: |-
      {
        "title": "Sala A",
        "description": "Sala de reuniones con proyector",
        "location": "Planta baja",
        "capacity": 8,
        "availableFrom": "08:00",
        "availableTo": "18:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
