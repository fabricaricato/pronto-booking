info:
  name: createUser
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4000/api/users
  body:
    type: json
    data: |-
      {
        "email": "bruno@example.com",
        "password": "password123",
        "name": "Bruno",
        "role": "user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
